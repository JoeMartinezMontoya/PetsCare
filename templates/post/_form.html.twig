{{ form_start(form) }}
<div class="row">
    <div class="col-9">{{ form_row(form.title) }}</div>
    <div class="col-3">{{ form_row(form.category) }}</div>
</div>
<div class="row">
    <div class="col">{{ form_row(form.content) }}</div>
</div>
<button class="btn btn-success">{{ button_label|default('Publier') }}</button>
{{ form_end(form) }}
