{% extends 'base.html.twig' %}

{% block title %}PetsCare | Ce sont eux ?{% endblock %}

{% block body %}
    <div class="jumbotron bg-transparent">
        <h1 class="text-center mb-4">Des animaux semblables ont disparus</h1>
        <div class="card-deck justify-content-center">
            {% for pet in missingPets %}
                {{ include('informations/_card_animal.html.twig') }}
            {% endfor %}
        </div>
    </div>
{% endblock %}